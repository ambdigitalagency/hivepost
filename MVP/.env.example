# NextAuth (Google 登录)
# 在 Google Cloud Console 创建 OAuth 2.0 客户端后填入
# 配置步骤见 docs/Google-OAuth-Setup.md
# Dev server runs on port 3001 (see package.json scripts.dev)
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your-random-secret-at-least-32-chars
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Supabase（数据库 + 后续 Storage）
# 在 Supabase 项目 Settings → API 获取；建表见 docs/Supabase-Setup.md
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI（Gatekeeper 意图校验、策略与文案生成）
OPENAI_API_KEY=sk-your-openai-api-key

# Replicate（候选图生成，阶段 6）
# 在 https://replicate.com/account/api-tokens 创建
REPLICATE_API_TOKEN=

# Supabase Storage：在 Dashboard → Storage 新建 bucket 名为 post-images（可选公开读）
# 图片生成需同时配置 REPLICATE_API_TOKEN 和 post-images bucket
